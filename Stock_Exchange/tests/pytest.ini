[pytest]
# Директория с тестами
testpaths = tests

# Паттерны обнаружения тестов
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Опции по умолчанию
addopts =
    -v
    --strict-markers
    --tb=short
    -ra